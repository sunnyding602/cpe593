class Tire{
    private:

pubilc:
    Trie(){}
    ~Trie(){}
int 0000000110000000
            
        class Node{
    Node*next[26];
        
        }


        void add(const string & word);
        bool contains(const string & word){
            if s[0] is null
                return false
                    Node *p = root[s[0]]
                    while(...){
                    
                    }
            return p->isWord;
        
        }

        //not very encapsulated, but lets us have access to nodes in the middle of the pre
        Node * startWith(const char s[]){
        
        }


}
